#include<stdio.h>
int main(){
    printf("Enter the age:")
    scanf("%d",&age);
    if(age >= 18 ){
        printf("elegible to vote");
    }else{
        printf("not elegible");
    }
    return 0;
}
